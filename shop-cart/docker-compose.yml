version: "3.8"
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: "512M"
